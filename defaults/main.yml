---
k8s_base_cidr: "192.168.0.0/24"
# k8s_base_node_ip: ""  # must be set and be unique for every node
k8s_base_node_name: "{{ inventory_hostname }}"   # must be unique for every node

#k8s_base_join_command_filename: "k8s-join-command"
